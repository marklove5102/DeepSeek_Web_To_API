# WebUI 开发服务器环境变量模板
#
# 仅供本地 `npm run dev --prefix webui` 时使用：复制为 webui/.env.development.local。
# 生产构建（npm run build）后由 Go 后端直接托管 static/admin/，无需此文件。

VITE_DEEPSEEK_WEB_TO_API_BACKEND_URL=http://127.0.0.1:5001
